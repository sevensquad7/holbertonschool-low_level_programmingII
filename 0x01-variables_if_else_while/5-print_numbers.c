#include <stdio.h>
/**
 * main - program that prints all single digit numbers of base 10 starting from 0
 * followed by a new line.
 * Return: 0
 */

int main(void)
{
	int ab;

	for (ab = 48; ab <= 57; ab++)
	{
		putchar(ab);
	}
	putchar('\n');

	return (0);
}
