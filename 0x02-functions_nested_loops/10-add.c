#include "holberton.h"

/**
 * add - function that adds two integers and returns the result.
 *@a:first value
 *@b:second value
 * Return: add return sum of values a + b.
 *
 */
int add(int a, int b)
{
	return (a + b);
}
